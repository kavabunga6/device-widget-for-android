<UserControl x:Class="AndroidWidget.Presentation.Chrome.DialogTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             Height="44" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
    <Grid Margin="14,0,10,0" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Image Width="18" Height="24" Margin="0,0,9,0" Source="{StaticResource AppIconImage}" />
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="11" Foreground="{DynamicResource TextSecondary}" />
            <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="9" Foreground="{DynamicResource TextSecondary}" Opacity="0.72" />
        </StackPanel>
        <Button Grid.Column="2" Width="32" Height="32" Padding="0" Margin="0"
                Style="{StaticResource IconButton}" ToolTip="Закрыть"
                shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseButton_Click">
            <Path Data="M 0,0 L 8,8 M 8,0 L 0,8" Stroke="{DynamicResource TextPrimary}"
                  StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                  HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Button>
    </Grid>
</UserControl>
